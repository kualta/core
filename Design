core::Window _mainWindow; <-- Should be a pointer
_mainWindow = core::CreateWindow();
_mainWindow.SetContext(GameMainFunc()); <-- A delegate of some sort?
                            /\ Game logic loop
_mainWindow.context.SetContent(GameRenderingLoop()); <-- wtf how it works??



// Maybe change _mainWindow calls to StartCapture commands or smth? <-- No??? Are you dumb? stupid + not gonna work