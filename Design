core::Window _mainWindow;
_mainWindow = core::CreateWindow();
_mainWindow.SetContext(GameMainFunc()); <-- A delegate of some sort?
                            /\ Game logic loop
_mainWindow.context.SetContent(GameRenderingLoop()); <-- wtf how it works??



// Maybe change _mainWindow calls to WindowStartCaptureCommands or smth?